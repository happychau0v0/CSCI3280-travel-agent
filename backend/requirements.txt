# Direct dependencies. Install with `-c requirements.lock` to reproduce the
# validated environment used by CI and Docker.
fastapi
uvicorn[standard]
openai
httpx
socksio
python-dotenv
pydantic
fast-flights
json-repair
weasyprint
jinja2
ruff
pytest
pytest-asyncio
